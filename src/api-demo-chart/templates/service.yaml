apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-demo-chart.fullname" . }}
  labels:
    {{- include "api-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-demo-chart.selectorLabels" . | nindent 4 }}
